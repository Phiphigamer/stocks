# ---- Required for the AI Forecast Engine ----
# Get a key at https://console.mistral.ai/ and add it in Vercel:
#   Project → Settings → Environment Variables → MISTRAL_API_KEY
MISTRAL_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ---- Optional ----
# The live market dashboard works WITHOUT any key (it uses Yahoo Finance's
# public endpoints). No financial-data API key is required.
